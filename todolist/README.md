# Stylish Animated To-Do List

A modern, visually appealing, and animated to-do list application built with React. It features a smooth, glassy interface with a dynamic gradient background and fluid animations for a great user experience.

**Live Demo:** [View Live Application](https://your-project-name.vercel.app)
*(You can replace the placeholder link above with a real screenshot of your app!)*

## ✨ Features

- **Add Tasks**: Quickly add new tasks to your list.
- **Complete Tasks**: Mark tasks as complete with a single click.
- **Edit Tasks**: Seamlessly edit existing tasks inline.
- **Delete Tasks**: Remove tasks you no longer need.
- **Stunning UI**: A beautiful "glassmorphism" design with a live animated gradient background.
- **Smooth Animations**: All interactions, from adding to deleting, are enhanced with CSS animations.
- **Responsive Design**: The layout is optimized for a great experience on various screen sizes.

## 🛠️ Technologies Used

- **React**: For building the user interface.
- **React Hooks (`useState`)**: For managing component state.
- **CSS3**: For all styling, including advanced features like `backdrop-filter`, `keyframes` for animations, and gradients.
- **HTML5**

## 🚀 Getting Started

To get a local copy up and running, follow these simple steps.

### Prerequisites

Make sure you have Node.js and npm installed on your machine.
- Node.js

### Installation & Setup

1. **Clone the repository:**
   ```sh
   git clone <your-repository-url>
   ```
2. **Navigate to the project directory:**
   ```sh
   cd todolist
   ```
3. **Install NPM packages:**
   ```sh
   npm install
   ```
4. **Run the application:**
   ```sh
   npm start
   ```
   The app will open automatically in your default browser at `http://localhost:3000`.